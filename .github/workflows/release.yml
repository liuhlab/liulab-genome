name: Release

on:
  # NEVER `push: tags:`. A version cannot be unpublished and cannot be reused, and a tag
  # trigger puts no human step between a tag reaching the remote and the upload — so one
  # mistyped or experimental tag ships a release nobody can take back. Publishing a GitHub
  # Release is a deliberate act by a person, so that is the trigger.
  release:
    types: [published]
  # For a release that failed on something outside the code — a missing trusted publisher,
  # an index outage. Choose the tag when you run it.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Build distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0   # hatch-vcs reads the tag from history
      - uses: prefix-dev/setup-pixi@v0.10.2
        with:
          environments: default
          locked: true
      - name: pixi run build
        run: pixi run -e default build
      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
          if-no-files-found: error

  publish:
    name: Publish to PyPI
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/liulab-genome
    permissions:
      contents: read
      id-token: write   # required for PyPI OIDC trusted publishing
    steps:
      - uses: actions/download-artifact@v8
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
